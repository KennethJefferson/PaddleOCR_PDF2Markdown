# Flask web framework
flask>=3.0.0
werkzeug>=3.0.0

# PaddleOCR and dependencies - SPECIFIC VERSIONS THAT WORK
paddleocr==2.8.1
paddlepaddle-gpu==2.5.2

# Image processing
opencv-python>=4.6.0
opencv-contrib-python>=4.10.0
Pillow>=10.0.0

# OCR dependencies
shapely>=2.1.0
scikit-image>=0.25.0
imgaug>=0.4.0
pyclipper>=1.3.0
lmdb>=1.7.0
rapidfuzz>=3.14.0

# Document processing
python-docx>=1.2.0
beautifulsoup4>=4.12.0

# Utilities
numpy<2.0  # Required for compatibility with PaddleOCR 2.8.1
scipy>=1.15.0
matplotlib>=3.10.0
tqdm>=4.67.0
pyyaml>=6.0.0
requests>=2.31.0
fonttools>=4.24.0
fire>=0.3.0

# IMPORTANT NOTES:
# 1. This server uses PaddleOCR 2.8.1 with PPStructure (not PPStructureV3)
# 2. Must use PaddlePaddle-GPU 2.5.2 for compatibility
# 3. NumPy must be <2.0 for compatibility
# 4. For CPU-only, replace paddlepaddle-gpu with paddlepaddle==2.5.2